# URL of the deployed Worker (or http://localhost:8787 for local dev)
VITE_API_URL=https://avery-platform.<account>.workers.dev

# Must match AVERY_SECRET wrangler secret
VITE_AVERY_SECRET=dev-avery-secret
