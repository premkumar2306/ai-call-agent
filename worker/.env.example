# Local dev only — use `wrangler secret put` for production
# These are NOT read by the Worker at runtime; they're here for documentation.

TOKEN_SECRET=dev-secret-minimum-32-characters-long
TOKEN_TTL_SECONDS=900
CUSTOMER_HASH_SALT=dev-hash-salt
AVERY_SECRET=dev-avery-secret
ANTHROPIC_API_KEY=sk-ant-...
RESEND_API_KEY=re_...
EMAIL_FROM=Avery <avery@yourstore.example.com>
TWILIO_AUTH_TOKEN=
TWILIO_WEBHOOK_URL=https://avery-platform.<account>.workers.dev
